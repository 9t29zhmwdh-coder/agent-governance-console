apiVersion: v1
kind: Service
metadata:
  name: {{ include "agc.fullname" . }}
  labels:
    {{- include "agc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agc.selectorLabels" . | nindent 4 }}
